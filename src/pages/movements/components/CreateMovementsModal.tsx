interface Props {
  isOpen: boolean;
  onOpenChange: () => void;
}

export const CreateMovementsModal: React.FC<Props> = ({
  isOpen,
  onOpenChange,
}) => {
  return <div>CreateMovementsModal</div>;
};
